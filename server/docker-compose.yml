version: '3.8'

services:
  backend_service:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3002:3002"
    environment:
      - NODE_ENV=production
    volumes:
      - .:/usr/src/app
    networks:
      - shared-network

networks:
  shared-network:
    driver: bridge